#include <stdio.h>
#include <stdlib.h>


// by default, the function called main is the first executed function
// in C/C++, we need to make clear the type of the function's return
void main(){
    // this is how to make prints with C
    printf("Hello, world");
}
